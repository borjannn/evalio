Django>=6.0.6
djangorestframework
djangorestframework-simplejwt
django-cors-headers
python-dotenv
psycopg[binary]

# AI-drafted feedback. Imported only inside feedback/providers/gemini.py, and only
# when AI_FEEDBACK_ENABLED is on — the app runs without ever loading it.
google-genai>=2.16.0

# DeepSeek provider (feedback/providers/deepseek.py). DeepSeek speaks the OpenAI
# wire format, so its provider uses the openai SDK pointed at DeepSeek's base URL.
# Imported lazily inside the provider on the same terms as google-genai above.
openai>=1.0.0

# dev
ruff
