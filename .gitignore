/.idea
.env

# Personal Claude Code instructions
CLAUDE.local.md

# Machine-local agent permission cache
.claude/settings.local.json

# The working plans (FRONTEND_PLAN, BACKEND_PLAN, FRONTEND_BUILD_PLAN,
# PROJECT_ARCHITECTURE, prototype1) and the Figma Make export were deleted once
# `docs/` replaced them. Their ignore rules went with them: three of those files
# were gitignored, which meant CLAUDE.md and the skills cited a style spec a
# fresh clone never had. Everything in `docs/` is checked in, deliberately.


.venv/
venv/

# Python cache files
__pycache__/
*.pyc
*.pyo
*.pyd


# OS files
.DS_Store
Thumbs.db

# Logs
*.log

**/__pycache__/